<!DOCTYPE html>
<html>
<head>
  <title>Cloud Storage</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%=javascript_include_tag "https://www.authy.com/form.authy.min.js" %>
  <%=stylesheet_link_tag "https://www.authy.com/form.authy.min.css" %>

</head>
<body>
<% if admin_signed_in? %> 
  <header class="c-navbar u-mb-medium">
      <%= link_to image_tag("logo.png", :alt => "Image Description", :class => "c-navbar__brand"), root_path %>
                       
           <!-- Navigation items that will be collapes and toggle in small viewports -->
           <nav class="c-nav collapse" id="main-nav">
              <ul class="c-nav__list">
                  <li class="c-nav__item">
                    <%= link_to "Dashboard", root_path, class: 'c-nav__link' %>
                  </li>
                  <li class="c-nav__item">
                      <div class="c-field c-field--inline has-icon-right u-hidden-up@tablet">
                          <span class="c-field__icon">
                              <i class="fa fa-search"></i> 
                          </span>
                      </div>
                  </li>
              </ul>
          </nav>
            <!-- // Navigation items  -->

            <div class="c-field c-field--inline has-icon-right u-hidden-down@tablet u-ml-auto u-mr-small">
                <% if current_admin %>
                  <label>Hello <%= current_admin.email %></label>
                <% else %>
                <% end -%>
            </div>
            
            <div class="c-dropdown dropdown">
                 <% if defined?(current_admin) %>
                      <% if current_admin %>
                        <%= link_to "Sign out", destroy_admin_session_path %></li>
                      <% else %>
                      <% end -%>
                  <% end -%>
            </div>

            <button class="c-nav-toggle" type="button" data-toggle="collapse" data-target="#main-nav">
                <span class="c-nav-toggle__bar"></span>
                <span class="c-nav-toggle__bar"></span>
                <span class="c-nav-toggle__bar"></span>
            </button><!-- // .c-nav-toggle -->
        </header>

<% else %>
      <header class="c-navbar u-mb-medium">
      <%= link_to image_tag("logo.png", :alt => "Image Description", :class => "c-navbar__brand"), root_path %>
                       
           <!-- Navigation items that will be collapes and toggle in small viewports -->
           <nav class="c-nav collapse" id="main-nav">
              <ul class="c-nav__list">
                  <li class="c-nav__item">
                    <%= link_to "My Storage", root_path, class: 'c-nav__link' %>
                  </li>
                  <li class="c-nav__item">
                      <div class="c-field c-field--inline has-icon-right u-hidden-up@tablet">
                          <span class="c-field__icon">
                              <i class="fa fa-search"></i> 
                          </span>
                      </div>
                  </li>
              </ul>
          </nav>
            <!-- // Navigation items  -->

            <div class="c-field c-field--inline has-icon-right u-hidden-down@tablet u-ml-auto u-mr-small">
                <% if current_user %>
                  <label>Hello <%= current_user.email %></label>
                <% else %>
                <% end -%>
            </div>
            
            <div class="c-dropdown dropdown">
                 <% if defined?(current_user) %>
                      <% if current_user %>
                        <%= link_to "Sign out", destroy_user_session_path %></li>
                      <% else %>
                      <% end -%>
                  <% end -%>
            </div>

            <button class="c-nav-toggle" type="button" data-toggle="collapse" data-target="#main-nav">
                <span class="c-nav-toggle__bar"></span>
                <span class="c-nav-toggle__bar"></span>
                <span class="c-nav-toggle__bar"></span>
            </button><!-- // .c-nav-toggle -->
        </header>
    
<% end %>
  <main class="container">

  
    <%= yield %>
  </main>
</body>
</html>
