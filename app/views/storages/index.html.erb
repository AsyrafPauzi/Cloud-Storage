<% if current_user.authy_enabled == false %>
         
<div class="jumbotron">
  <H1><a class="button2" <%= link_to "Verify Number First",user_enable_authy_path , class: "buttton2" , onClick:"window.location.reload()" %></a><h1>
  
  <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book.</p>
  
</div>
         <% else %>       
<h1>Upload</h1>
<%= link_to "New File", new_storage_path, class: "btn btn-primary" %>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Download Link</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @files.each do |file| %>
      <tr>
        <td><%= file.name %></td>
        <td><%= link_to "Download File", download_storage_path(file), download: download_storage_path(file), target: "_blank" %></td>
        <td><%= button_to "Delete",  file, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{file.name}?" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
